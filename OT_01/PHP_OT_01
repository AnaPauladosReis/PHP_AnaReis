OT_01

Seção PHP - Exercícios Teóricos:

1. Explique a diferença entre variáveis do tipo string e do tipo inteiro em PHP.

	R: O php distingue difernetes tipos de valor que você pode pode armazenar em uma variável (como número e texto); isso é conhecido tipos de dados🧮
	-uma parte de um texto de se chama string.
	-um número sem parte decimal se chama inteiro.
	*A principal diferença é que strings representam texto enquanto inteiros representam números, e o PHP faz conversões automáticas entre eles em muitos contextos, o que pode ser tanto útil quanto perigoso se não for entendido corretamente.
	
2. Quais são os operadores lógicos mais comuns em PHP e como eles são utilizados
em expressões condicionais?
	
	R: Os operadores lógicos mais comuns em PHP são essenciais para construir expressões condicionais complexas. Aqui estão os principais:
	1. AND / && (E lógico):
	// Ambas as condições devem ser verdadeiras
	if ($idade >= 18 && $tem_carteira == true) {
		echo "Pode dirigir";
	}

	// Equivalente usando AND
	if ($idade >= 18 AND $tem_carteira == true) {
		echo "Pode dirigir";
	}
	
	2.OR / || (OU lógico):
	// Pelo menos uma condição deve ser verdadeira
	if ($chovendo == true || $frio == true) {
		echo "Leve um casaco";
	}

	// Equivalente usando OR
	if ($chovendo == true OR $frio == true) {
		echo "Leve um casaco";
	}
	
	3. XOR (OU exclusivo):
	// Apenas uma das condições deve ser verdadeira (não ambas)
	if ($opcao_a == true XOR $opcao_b == true) {
		echo "Apenas uma opção foi selecionada";
	}
	
	4. ! (NÃO lógico):
	// Inverte o valor booleano
	if (!$usuario_logado) {
		echo "Faça login para continuar";
	}
	
	*Os operadores têm diferentes níveis de precedência. A ordem é:

	1 - ! (NOT)
	2 - && (AND)
	3 - || (OR)
	4 - and / or (têm menor precedência)
	
3. Como você definiria a tipagem dinâmica em PHP? Dê um exemplo.

	R: A tipagem dinâmica em PHP significa que as variáveis não têm um tipo fixo definido na declaração. O tipo é determinado automaticamente pelo valor atribuído e pode mudar durante a execução. 
	✅ Tipo inferido: PHP descobre o tipo pelo valor
	✅ Conversão implícita: Conversões automáticas entre tipos
	✅ Flexibilidade: Mesma variável pode armazenar diferentes tipos
	✅ Sem declaração de tipo: Não precisa especificar tipo na criação
	
	Exemplo Prático:
	
	<?php
	// Variável sem tipo definido
	$dado = 10;           // Agora é inteiro
	echo gettype($dado);  // Output: integer
	echo $dado;           // Output: 10

	// Mudando o tipo dinamicamente
	$dado = "Hello";      // Agora é string
	echo gettype($dado);  // Output: string
	echo $dado;           // Output: Hello

	$dado = 3.14;         // Agora é float
	echo gettype($dado);  // Output: double
	echo $dado;           // Output: 3.14

	$dado = true;         // Agora é boolean
	echo gettype($dado);  // Output: boolean
	echo $dado;           // Output: 1 (true)
	?>
	
	Comparação com Tipagem Estática:
	
	<?php
	// PHP (tipagem dinâmica)
	$variavel = 10;       // integer
	$variavel = "texto";  // agora string - PERMITIDO

	// Em Java (tipagem estática) isso geraria erro:
	// int variavel = 10;
	// variavel = "texto"; // ERRO: incompatible types
	?>
	
	Vantagens e Desvantagens
	✅ Vantagens:
	Flexibilidade: Código mais adaptável
	Desenvolvimento rápido: Menos código boilerplate
	Fácil aprendizado: Menos conceitos para iniciantes
	
	❌ Desvantagens:
	Erros em tempo de execução: Alguns erros só aparecem quando executados
	Performance: Conversões implicitas têm custo
	Menos previsível: Pode ter comportamentos inesperados
	
	A tipagem dinâmica torna PHP muito flexível, mas requer atenção para evitar comportamentos inesperados devido às conversões automáticas.
	
4. Explique o propósito da estrutura de controle if e forneça um exemplo de uso.

	R: O if é uma estrutura de controle condicional que permite executar blocos de código apenas se uma condição específica for verdadeira. É fundamental para:
	✅ Tomar decisões no código
	✅ Controlar o fluxo de execução
	✅ Validar condições antes de executar ações
	✅ Criar lógicas complexas baseadas em regras

	sintaxe ásica:
	if (condição) {
    // código executado se a condição for TRUE
	}
	
	exemplo prático de verificação simples:
	<?php
	$idade = 18;

	if ($idade >= 18) {
		echo "Você é maior de idade!\n";
		echo "Pode acessar o conteúdo restrito.";
	}
	// Output: Você é maior de idade! Pode acessar o conteúdo restrito.
	?>
	
5. O que é o operador de módulo (%) e como ele pode ser útil em PHP?

	R:O operador de módulo (%) retorna o resto da divisão entre dois números. Ele é muito útil para verificar divisibilidade, criar padrões cíclicos e resolver problemas matemáticos.
	
	sintaxe básica:
	$resto = $dividendo % $divisor;
	
	exemplos fundamentais:
	<?php
	echo 10 % 3;   // 1 (10 ÷ 3 = 3, resto 1)
	echo 15 % 4;   // 3 (15 ÷ 4 = 3, resto 3)
	echo 20 % 5;   // 0 (20 ÷ 5 = 4, resto 0 - divisão exata)
	echo 7 % 2;    // 1 (7 ÷ 2 = 3, resto 1 - número ímpar)
	?>
	
	Aplicações Práticas Úteis
	1. Verificar Par ou Ímpar:
	
	<?php
	$numero = 15;

	if ($numero % 2 == 0) {
		echo "$numero é PAR";
	} else {
		echo "$numero é ÍMPAR";
	}
	// Output: 15 é ÍMPAR
	?>
	
	Casos de Uso Comuns
	✅ Verificação de paridade (par/ímpar)
	✅ Cálculo de ciclos e repetições
	✅ Distribuição uniforme de elementos
	✅ Validação de algoritmos matemáticos
	✅ Controle de fluxo baseado em padrões
	✅ Geração de sequências periódicas
	O operador de módulo é uma ferramenta poderosa para resolver problemas de forma elegante e eficiente em PHP
	
6. Qual é a importância do ponto e vírgula (;) no final de uma instrução PHP?

	R:O ponto e vírgula (;) é um delimitador de instruções em PHP. Ele marca o final de cada comando, indicando onde uma instrução termina e a próxima começa.
	
	sintaxe básica:
	<?php
	echo "Hello World";  // ← ponto e vírgula no final
	$nome = "João";      // ← ponto e vírgula no final
	?>
	
	O ponto e vírgula é essencial para a sintaxe do PHP. Embora existam algumas exceções, a melhor prática é sempre usá-lo no final de cada instrução para evitar erros e manter o código consistente.
	
7. Descreva o funcionamento da estrutura de controle switch e forneça um exemplo
prático.

	R:O switch é uma estrutura de controle que compara uma variável com múltiplos valores possíveis e executa diferentes blocos de código dependendo da correspondência. É uma alternativa mais limpa a múltiplos if-elseif quando se testa a mesma variável contra vários valores.
	
	sintaxe:
	switch ($variavel) {
    case valor1:
        // código se $variavel == valor1
        break;
        
    case valor2:
        // código se $variavel == valor2
        break;
        
    default:
        // código se nenhum caso corresponder
	}
	
	exemplo prático básico:
	<?php
	$dia_semana = 3;

	switch ($dia_semana) {
		case 1:
        echo "Domingo - Fim de semana";
        break;
        
    case 2:
        echo "Segunda-feira - Dia útil";
        break;
        
    case 3:
        echo "Terça-feira - Dia útil";
        break;
        
    case 4:
        echo "Quarta-feira - Dia útil";
        break;
        
    case 5:
        echo "Quinta-feira - Dia útil";
        break;
        
    case 6:
        echo "Sexta-feira - Dia útil";
        break;
        
    case 7:
        echo "Sábado - Fim de semana";
        break;
        
    default:
        echo "Dia inválido!";
	}
	// Output: Terça-feira - Dia útil
	?>
	
	Características Importantes
	
	1. O break é Crucial
	<?php
	$nota = "B";

	switch ($nota) {
    case "A":
        echo "Excelente! ";
        // SEM break - continua executando
    case "B":
        echo "Bom! ";
        // SEM break - continua executando
    case "C":
        echo "Regular ";
        // SEM break - continua executando
    case "D":
        echo "Reprovado";
        break;
	}
	// Output: Bom! Regular Reprovado
	?>
	
	Conclusão
	O switch é ideal para:
	✅ Comparar a mesma variável contra múltiplos valores
	✅ Substituir longas cadeias de if-elseif
	✅ Criar código mais limpo e legível
	✅ Implementar sistemas de permissões hierárquicas
	Use quando precisar testar uma variável contra valores específicos e prefira if-elseif para condições complexas ou múltiplas variáveis.
	
8. Por que é importante escolher nomes de variáveis significativas ao programar em
PHP?

	R:Legibilidade e Compreensão Imediata
	
	Exemplo:
	// ❌ RUIM - O que são essas variáveis?
	$a = 10;
	$b = 20;
	$c = $a * $b;

	// ✅ BOM - Significado claro
	$quantidade_produtos = 10;
	$preco_unitario = 20;
	$total_venda = $quantidade_produtos * $preco_unitario;
	
	Impacto no Trabalho em Equipe
	Código Colaborativo
	
	Exemplo:
	
	// ❌ Difícil para outros desenvolvedores
	function proc($d) {
		// O que essa função faz?
	}

	// ✅ Fácil para qualquer desenvolvedor
	function processarPedido($dados_pedido) {
		// Processa um pedido de compra
		// $dados_pedido contém: id, produtos, total, etc.
	}
	
	Onboarding de Novos Desenvolvedores
	
	// ❌ Requer documentação extra
	$lst = []; // Lista de what?

	// ✅ Autoexplicativo
	$lista_produtos_carrinho = [];
	$ids_usuarios_ativos = [];
	
	
	Exemplo de Refatoração
	
	Antes (Código Confuso):
	<?php
	$n = $_POST['n'];
	$e = $_POST['e'];
	$p = $_POST['p'];

	$v = validar($n, $e, $p);

	if ($v) {
		$r = registrar($n, $e, $p);
		if ($r) {
			echo "OK";
		}
	}
	?>
	
	Depois (Código Claro):
	<?php
	$nome_usuario = $_POST['nome'];
	$email_usuario = $_POST['email'];
	$senha_usuario = $_POST['senha'];

	$dados_validos = validarUsuario($nome_usuario, $email_usuario, $senha_usuario);

	if ($dados_validos) {
		$registro_sucesso = registrarUsuario($nome_usuario, $email_usuario, $senha_usuario);
		if ($registro_sucesso) {
			echo "Usuário registrado com sucesso!";
		}
	}
	?>
	
	Nomes significativos de variáveis são investimento, não gasto. O tempo gasto pensando em bons nomes é recuperado multiplicadamente durante a manutenção, debugging e expansão do código.
	
9. Como você concatenaria duas strings em PHP?

	R: Em PHP, o operador para concatenar strings é o ponto (.). Diferente de outras linguagens que usam o sinal de mais (+), no PHP o ponto é específico para unir strings.
	
	sintaxe basica:
	$string_final = $string1 . $string2;
	
	exemplo prático:
	<?php
	$nome = "João";
	$sobrenome = "Silva";

	// Concatenando duas strings
	$nome_completo = $nome . $sobrenome;
	echo $nome_completo; // Output: JoãoSilva

	// Com espaço entre as strings
	$nome_completo = $nome . " " . $sobrenome;
	echo $nome_completo; // Output: João Silva
	?>
	
	metodo avançado:
	Operador de Atribuição com Concatenção (.=)
	<?php
	$frase = "Olá";
	$frase .= " mundo"; // Equivalente a: $frase = $frase . " mundo"
	echo $frase; // Output: Olá mundo

	// Útil para construir strings gradualmente
	$html = "";
	$html .= "<div>";
	$html .= "<h1>Título</h1>";
	$html .= "<p>Conteúdo</p>";
	$html .= "</div>";
	echo $html;
	?>
	
10. Explique o conceito de variáveis de tipo booleano e forneça um exemplo de
utilização.

	R:Variáveis booleanas são um tipo de dado primitivo que pode armazenar apenas dois valores:

	- true (verdadeiro)
	- false (falso)
	
	Elas são essenciais para controle de fluxo e tomada de decisões em programação.
	
	sintaxe básica:
	
	<?php
	$verdadeiro = true;
	$falso = false;

	// Também pode ser em maiúsculas (case-insensitive)
	$ativo = TRUE;
	$inativo = FALSE;
	?>
	
	Valores que Convertem para Boolean
	
	Convertem para false:
	- false (próprio booleano)
	- 0 (inteiro zero)
	- 0.0 (float zero)
	- "" (string vazia)
	- "0" (string zero)
	- [] (array vazio)
	- null

	Convertem para true:
	- Qualquer outro valor
	
	Variáveis booleanas são fundamentais para:

	✅ Controle de fluxo (if, while, for)
	✅ Validação de condições e regras
	✅ Controle de estado de processos
	✅ Configurações e flags do sistema
	✅ Tomada de decisões lógicas

	Elas tornam o código mais expressivo e legível, especialmente quando usadas com nomes descritivos que indicam claramente sua função no sistema.
	
	
	


	